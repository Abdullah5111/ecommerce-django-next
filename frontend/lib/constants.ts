/** Free shipping over this subtotal — mirrors the backend FREE_SHIPPING_THRESHOLD. */
export const FREE_SHIPPING_THRESHOLD = 50;

/** Products per page — mirrors the backend REST_FRAMEWORK PAGE_SIZE. */
export const PAGE_SIZE = 12;
